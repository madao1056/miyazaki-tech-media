---
import { OTHER_LINKS } from "@/lib/config";
---

<footer
  class="border-t border-base-300 py-8 bg-base-200"
>
  <div class="container max-w-7xl mx-auto px-4">
    <div class="flex flex-col items-center gap-4 text-center">
      <p class="text-sm text-base-content/70">
        <strong class="text-base">みやびと</strong> - 宮崎で輝く人々の物語をつなぐストーリーメディア
      </p>
      <nav class="flex flex-wrap justify-center gap-4 text-xs">
        {
          OTHER_LINKS.map(({ href, text, target }) => (
            <a href={href} class="link link-hover text-base-content/60" target={target ?? "_self"}>
              {text}
            </a>
          ))
        }
      </nav>
      <p class="text-xs text-base-content/50">
        © 2025 合同会社ぼんど. All rights reserved.
      </p>
    </div>
  </div>
</footer>
