---
import type { Icon } from "@/lib/types";

type Props = Icon;

const {
  width = "20",
  height = "20",
  size,
} = Astro.props;
---

<img
  src="/icons/x-logo.svg"
  alt="X (Twitter)"
  width={size ? size : width}
  height={size ? size : height}
  class="x-logo-icon invert dark:invert-0"
/>
