---
import { SITE } from "@/lib/config";
import Head from "@/components/bases/head.astro";
import Header from "@/components/shared/header.astro";
import Footer from "@/components/shared/footer.astro";
import BannerSection from "@/components/shared/banner-section.astro";
import AuthorModal from "@/components/shared/author-modal.astro";
import ScrollToTop from "@/components/elements/scroll-to-top.astro";
import SpeedInsights from "@vercel/speed-insights/astro";
import type { Entry } from "@/lib/types";
import { getMeta } from "@/lib/utils/getMeta";

type Props = {
  entry: Entry;
};

const { entry } = Astro.props;

const meta = await getMeta(entry);
---

<!doctype html>
<html lang={SITE.locale} dir={SITE.dir}>
  <Head meta={meta} />
  <body class="flex flex-col">
    <!-- Google Tag Manager (noscript) - Lazy loaded -->
    <noscript id="gtm-noscript-placeholder"></noscript>
    <!-- End Google Tag Manager (noscript) -->
    
    <Header />
    <main class="flex-1">
      <slot />
    </main>
    <BannerSection />
    <Footer />
    <AuthorModal />
    <ScrollToTop />
    <SpeedInsights />
    
    <!-- GTM noscript lazy loading -->
    <script>
    window.addEventListener('load', function() {
      setTimeout(function() {
        const placeholder = document.getElementById('gtm-noscript-placeholder');
        if (placeholder) {
          placeholder.innerHTML = '<iframe src="https://www.googletagmanager.com/ns.html?id=GTM-NFDHSBNS" height="0" width="0" style="display:none;visibility:hidden"></iframe>';
        }
      }, 1000);
    });
    </script>
  </body>
</html>
